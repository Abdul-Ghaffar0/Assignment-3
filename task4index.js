// Declare a variable called isRaining and assign it a value of true
let isRaining = true;

// Check if it is raining using an if...else statement
if (isRaining) {
  console.log("Bring an umbrella");
} else {
  console.log("Leave the umbrella at home");
}